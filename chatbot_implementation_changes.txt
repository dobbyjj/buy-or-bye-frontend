챗봇 기능 구현 중 변경사항 요약:

1. 파일 생성 및 이름 변경:
   - src/pages/ChatbotPage.jsx:
     수정전: (blank)
     수정후: 챗봇 UI 및 프론트엔드 로직을 담은 파일 생성

   - server.cjs:
     수정전: (blank) (원래 server.js로 생성 후 server.cjs로 이름 변경)
     수정후: 백엔드 서버 로직 (OpenAI API 프록시) 파일 생성 및 이름 변경

2. 파일 내용 수정:
   - .gitignore:
     수정전: /dist, /node_modules
     수정후: /dist, /node_modules, .env (API 키 보안을 위해 .env 추가)

   - src/App.jsx:
     수정전: <Route path="/chatbot" element={<div>챗봇 (하단4)</div>} />
     수정후: <Route path="/chatbot" element={<ChatbotPage />} /> (챗봇 페이지 컴포넌트 연결)

   - server.cjs (이전 server.js):
     수정전: import express from 'express'; ... (ES module import 문법)
     수정후: const express = require('express'); ... (CommonJS require 문법으로 변경)

3. 새로 설치된 npm 패키지:
   - openai
   - express
   - cors
   - dotenv